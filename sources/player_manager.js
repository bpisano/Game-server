class PlayerManager {

    static createPlayer(id, username) {
        const sql = require("./sql")
        return sql.insertInto("players", `\`\``)
        return new Promise((resolve, reject) => {
            
        })
    }

}
